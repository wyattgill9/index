# Point git at the repo's tracked hooks directory.
# `direnv allow` is enough; no `nix develop` shell needed.
export GIT_CONFIG_COUNT=1
export GIT_CONFIG_KEY_0=core.hooksPath
export GIT_CONFIG_VALUE_0=.githooks
